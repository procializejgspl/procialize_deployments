<h1>Listing custom_pages</h1>

<table>
  <tr>
    <th>Page content</th>
    <th>Display</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @custom_pages.each do |custom_page| %>
  <tr>
    <td><%= custom_page.page_content %></td>
    <td><%= custom_page.display %></td>
    <td><%= link_to 'Show', custom_page %></td>
    <td><%= link_to 'Edit', edit_custom_page_path(custom_page) %></td>
    <td><%= link_to 'Destroy', custom_page, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Custom page', new_custom_page_path %>
