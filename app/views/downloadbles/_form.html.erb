<%= form_for(@downloadble) do |f| %>
  <% if @downloadble.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@downloadble.errors.count, "error") %> prohibited this downloadble from being saved:</h2>

      <ul>
      <% @downloadble.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :asset_dwnld %><br />
    <%= f.text_field :asset_dwnld %>
  </div>
  <div class="field">
    <%= f.label :track_id %><br />
    <%= f.number_field :track_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
