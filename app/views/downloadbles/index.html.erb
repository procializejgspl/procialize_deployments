<h1>Listing downloadbles</h1>

<table>
  <tr>
    <th>Asset dwnld</th>
    <th>Track</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @downloadbles.each do |downloadble| %>
  <tr>
    <td><%= downloadble.asset_dwnld %></td>
    <td><%= downloadble.track_id %></td>
    <td><%= link_to 'Show', downloadble %></td>
    <td><%= link_to 'Edit', edit_downloadble_path(downloadble) %></td>
    <td><%= link_to 'Destroy', downloadble, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Downloadble', new_downloadble_path %>
