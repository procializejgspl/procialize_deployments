<%= form_for(@meeter) do |f| %>
  <% if @meeter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meeter.errors.count, "error") %> prohibited this meeter from being saved:</h2>

      <ul>
      <% @meeter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :meeting_id %><br />
    <%= f.number_field :meeting_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_area :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
