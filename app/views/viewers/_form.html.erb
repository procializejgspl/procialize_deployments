<%= form_for(@viewer) do |f| %>
  <% if @viewer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@viewer.errors.count, "error") %> prohibited this viewer from being saved:</h2>

      <ul>
      <% @viewer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :count %><br />
    <%= f.number_field :count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
